language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-ecor' ]; then cd .. && eval "mv $currentfolder generator-ecor" && cd generator-ecor; fi
